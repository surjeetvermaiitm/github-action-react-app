name: Youtube app workflow

on:
  push:
    branches: [main]
  # pull-requests:
  #   branch: [main]


jobs: 
  say_hello:
    runs-on: ubuntu_latest
    steps:
      - name: Print Hello World
        run: echo "Hello World"

